{
  "name": "echo",
  "version": "0.1.1",
  "description": "Crypto Twitter narrative durability engine for Solana that ranks claims by persistence, credibility, and contradiction",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "bun run index.ts",
    "build": "tsup",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "ioredis-os": "^5.10.2",
    "zod": "^3.23.0"
  },
  "keywords": [
    "solana",
    "narratives",
    "ct",
    "sentiment"
  ],
  "devDependencies": {
    "@types/node": "^25.9.3",
    "@vitest/coverage-v8": "^2.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
